packages:
  - 'packages/*'
  - 'apps/*'
  - 'tests/test-kit'

# Native/postinstall builds are opt-in. Only these may run install scripts.
allowBuilds:
  better-sqlite3-multiple-ciphers: true
  esbuild: true

onlyBuiltDependencies:
  - better-sqlite3-multiple-ciphers
  - esbuild
